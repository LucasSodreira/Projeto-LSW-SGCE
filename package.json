{
  "name": "sgce",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Gerenciamento de Competições Esportivas",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "http-server": "^14.1.1",
    "json-server": "^1.0.0-beta.3"
  },
  "dependencies": {
    "axios": "^1.11.0"
  },
  "scripts": {
    "api": "json-server --watch dados.json --port 3000",
    "static": "http-server -c-1 -p 5173 .",
    "dev": "concurrently \"npm:api\" \"npm:static\"",
    "start": "npm run dev",
    "api:clean": "del db.json 2>NUL || true"
  }
}
